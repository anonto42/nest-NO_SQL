import { Injectable } from '@nestjs/common';

@Injectable()
export class DatabaseService {
  // You could add any database-related methods here in the future
  constructor() {
    // Database initialization logic (if needed)
  }
}
